function gridCreation (gridNumber) {
    let gridArea = gridNumber * gridNumber;
    for (i = 0; i < gridArea, i++){
        something select container div
        something create div 
        something append to div
    }
}